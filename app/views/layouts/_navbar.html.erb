<header class="navbar">
  <% if devise_user_signed_in? %>
    <div class="navbar__brand">
      <%= current_company.name %>
    </div>
    <div class="navbar__name">
      <%= current_devise_user.name %>
    </div>
    <%= button_to "Sign out",
                  destroy_devise_user_session_path,
                  method: :delete,
                  class: "btn btn--dark" %>
  <% else %>
    <%= link_to "Sign in",
                new_devise_user_session_path,
                class: "btn btn--dark navbar__right" %>
  <% end %>
</header>